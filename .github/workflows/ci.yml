name: CI-CD Cloth Store

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build step
        run: echo "Building cloth store project"

      - name: Deploy step
        run: echo "Deploying to GitHub Pages"


